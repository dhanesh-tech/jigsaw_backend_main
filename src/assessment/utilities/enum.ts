export enum ASSESSMENT_STATUS {
  draft = 'draft',
  publish = 'publish',
  archived = 'archived',
}

export enum SKILL_DIFFICULTY_LEVEL {
  beginner = 'beginner',
  intermediate = 'intermediate',
  expert = 'expert',
}
