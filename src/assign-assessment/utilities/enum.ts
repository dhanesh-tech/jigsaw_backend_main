export enum ASSESSMENT_ASSIGN_STATUS {
  assigned = 'assigned',
  pending = 'pending',
  accepted = 'accepted',
  declined = 'declined',
  rejected = 'rejected',
  shortlisted = 'shortlisted',
  completed = 'completed',
}
